#include <stdio.h>
void input()
{
    char *string;
    printf("enter a string\n");
    scanf("%s",string);
}
int string_length(char *string)
{
    int i;
    for(i=0;s[i]!='\0';i++)
        return 1;
}
int string_cmp(char *string,char *substring)
{
    for(int i=0;i<n-1 && string[i]==substring[i] &&string[i]!='\0';i++);
    return string[i]-substring[i];
}
void output(char *string,char *substring)
{
    printf("")
}